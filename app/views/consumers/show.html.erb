<div class="page-header">
  <h1>カート一覧<small>　ユーザー名：<%= current_consumer.nickname %>さん</small><h1>
</div>
<div class="container">
  <% if @posts.empty? %>
    <h2>まだカートに入れた商品はございません</h2>
  <% else %>
    <%= render 'layouts/image'%>
  <% end %>
</div>
<% if @posts.empty? %>
<% else %>
  <%= link_to "カート内の商品を注文する", new_order_path, class: "button" %>
<% end %>